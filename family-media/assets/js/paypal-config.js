/**
 * Optional PayPal-hosted Payment Links.
 *
 * Leave values blank to use the default "request a PayPal invoice" workflow.
 * Paste only public PayPal Payment Link URLs here. Never paste a password,
 * Client Secret, API secret, access token, or private merchant credential.
 */
window.CLINTWARE_PAYPAL = Object.freeze({
  mini: "",
  studio: "",
  creator: ""
});
