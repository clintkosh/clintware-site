import { Buffer } from 'node:buffer';

const PRODUCT = 'LandThePlane';
const MAX_FILE_BYTES = 12 * 1024 * 1024;
const MAX_TOTAL_BYTES = 22 * 1024 * 1024;
const REVIEW_MODEL = '@cf/meta/llama-3.1-8b-instruct-fast';
const TRANSCRIBE_MODEL = '@cf/openai/whisper-large-v3-turbo';

const securityHeaders = {
  'Content-Security-Policy': "default-src 'self'; script-src 'self' 'unsafe-inline'; style-src 'self' 'unsafe-inline'; img-src 'self' data: blob:; media-src 'self' blob:; connect-src 'self'; font-src 'self'; base-uri 'none'; frame-ancestors 'none'; form-action 'self'",
  'Referrer-Policy': 'strict-origin-when-cross-origin',
  'X-Content-Type-Options': 'nosniff',
  'X-Frame-Options': 'DENY',
  'Permissions-Policy': 'camera=(), microphone=(self), display-capture=(self), geolocation=(), browsing-topics=()'
};

function htmlResponse(body, status = 200) {
  return new Response(body, { status, headers: { ...securityHeaders, 'Content-Type': 'text/html; charset=utf-8' } });
}

function jsonResponse(data, status = 200) {
  return new Response(JSON.stringify(data), { status, headers: { ...securityHeaders, 'Content-Type': 'application/json; charset=utf-8', 'Cache-Control': 'no-store' } });
}

function pageShell(title, body, extraScript = '') {
  return `<!doctype html><html lang="en"><head><meta charset="utf-8"><meta name="viewport" content="width=device-width,initial-scale=1"><meta name="theme-color" content="#070a0f"><title>${title}</title><style>
:root{color-scheme:dark;--bg:#070a0f;--panel:#0b1017;--panel2:#0e151e;--line:#263442;--text:#eef6fb;--muted:#98aabc;--cyan:#6bdcff;--green:#70e2a8;--amber:#efc86a;--red:#ff8e8e;--font:"Cascadia Code","Segoe UI Mono",SFMono-Regular,Menlo,Consolas,"Liberation Mono",monospace}*{box-sizing:border-box}html{background:var(--bg);scroll-behavior:smooth}body{margin:0;background:radial-gradient(circle at 80% 0%,rgba(107,220,255,.06),transparent 30rem),var(--bg);color:var(--text);font:13px/1.58 var(--font)}a{color:var(--cyan)}button,textarea,select,input{font:inherit}.wrap{width:min(1120px,calc(100% - 32px));margin:auto}.top{display:flex;justify-content:space-between;gap:18px;padding:17px 0;border-bottom:1px solid var(--line);font-size:11px}.brand{font-weight:700;text-decoration:none;color:var(--text)}.brand b{color:var(--cyan)}.nav{display:flex;gap:14px;flex-wrap:wrap}.nav a{color:var(--muted);text-decoration:none}.hero{padding:42px 0 24px}.eyebrow,.label{font-size:10px;letter-spacing:.09em;text-transform:uppercase;color:var(--cyan)}h1{font-size:24px;line-height:1.3;letter-spacing:-.02em;margin:12px 0 5px;max-width:48ch}h2{font-size:16px;line-height:1.35;margin:0 0 10px}h3{font-size:13px;margin:0 0 8px}.tagline{font-size:16px;color:var(--cyan);margin:0 0 14px}.lede{max-width:900px;color:#b1c0ce;font-size:14px;margin:0}.status{display:inline-block;margin-top:17px;border:1px solid #285047;background:#0a1714;color:var(--green);padding:6px 8px;font-size:10px;text-transform:uppercase}.grid{display:grid;grid-template-columns:1fr 1fr;gap:12px}.panel{border:1px solid var(--line);background:var(--panel);padding:17px}.field{display:grid;gap:7px;margin-top:13px}.field label{color:#c8d5df;font-size:11px}.field small{color:#73879a}.field textarea{min-height:180px;resize:vertical;background:#06090d;color:var(--text);border:1px solid #2b3947;padding:12px;outline:none}.field textarea.compact{min-height:92px}.field textarea:focus,.field select:focus,.field input:focus{border-color:#4a7a8f}.field select,.field input[type=file],.field input[type=text]{background:#06090d;color:var(--text);border:1px solid #2b3947;padding:9px;width:100%}.actions{display:flex;gap:8px;flex-wrap:wrap;margin-top:14px}.btn{border:1px solid #315267;background:#0b1a23;color:#dff7ff;padding:9px 12px;cursor:pointer}.btn.primary{border-color:#2f7993;background:#0b2530}.btn.danger{border-color:#714040;background:#231111}.btn.ghost{background:transparent;color:var(--muted)}.btn:disabled{opacity:.45;cursor:not-allowed}.check{display:flex;align-items:flex-start;gap:8px;color:var(--muted);font-size:11px;margin-top:12px}.result{margin-top:12px;display:grid;gap:10px}.metricrow{display:grid;grid-template-columns:repeat(4,1fr);gap:8px}.metric{border:1px solid #23313e;background:#081019;padding:11px}.metric b{display:block;color:var(--cyan);font-size:18px}.metric span{color:var(--muted);font-size:10px}.item{border-left:2px solid #2b6075;padding:8px 10px;background:#081019}.item strong{display:block;font-size:11px}.item p{margin:5px 0 0;color:var(--muted);font-size:11.5px}.gap{border-left-color:#7c6330}.ok{border-left-color:#2f7857}.warn{border-left-color:#965d32}.muted{color:var(--muted)}.note{margin-top:12px;border:1px solid #3a3321;background:#151208;padding:11px;color:#c9b77c;font-size:11px}.error{border-color:#633030;background:#1b0d0d;color:#ffb0b0}.section{padding:28px 0;border-top:1px solid var(--line)}.cards{display:grid;grid-template-columns:repeat(3,1fr);gap:10px}.card{border:1px solid var(--line);background:var(--panel);padding:15px}.card p,.card li{color:var(--muted);font-size:11.5px}.card ul{padding-left:17px;margin-bottom:0}.footer{border-top:1px solid var(--line);padding:22px 0 30px;color:#718496;font-size:10.5px;display:flex;justify-content:space-between;gap:15px;flex-wrap:wrap}.history{margin-top:10px}.history div{border-top:1px solid #1c2833;padding:7px 0;color:var(--muted);font-size:10.5px}.hidden{display:none!important}.recordbar{display:flex;align-items:center;gap:10px;flex-wrap:wrap;padding:10px;border:1px solid #263442;background:#071018;margin-top:12px}.dot{width:9px;height:9px;border-radius:50%;background:#586675}.dot.live{background:#ff6868;box-shadow:0 0 0 5px rgba(255,104,104,.1)}.timer{font-weight:700;color:#dff7ff}.audioPreview{width:100%;margin-top:8px}.transcript{max-height:260px;overflow:auto;white-space:pre-wrap;border:1px solid #20303d;background:#06090d;padding:10px;color:#aebdca;font-size:11px}.splitTitle{display:flex;justify-content:space-between;gap:12px;align-items:flex-start}.privacyFlag{font-size:9px;color:var(--green);border:1px solid #285047;padding:3px 5px;text-transform:uppercase}@media(max-width:820px){.grid,.cards{grid-template-columns:1fr}.metricrow{grid-template-columns:repeat(2,1fr)}.hero{padding-top:34px}h1{font-size:20px}.field textarea{min-height:150px}.top{flex-direction:column}}@media(max-width:480px){.metricrow{grid-template-columns:1fr}}
</style></head><body>${body}${extraScript}</body></html>`;
}

function renderHome() {
  const body = `<header class="wrap top"><a class="brand" href="/">LAND<b>THE</b>PLANE</a><nav class="nav"><a href="#prep">Prep map</a><a href="#review">Interview review</a><a href="#product">Career OS</a><a href="/privacy/">Privacy</a><a href="https://www.clintware.com/tools/landtheplane/">Clintware ↗</a></nav></header>
<main><section class="wrap hero"><div class="eyebrow">CAREER OPERATING SYSTEM · PUBLIC ALPHA</div><h1>LandThePlane</h1><div class="tagline">And Hit the Ground Rolling.</div><p class="lede">Build interview preparation from evidence you can defend, record or upload an interview for review, and carry the same career graph forward into ramping, performance, proof, and future pivots.</p><div class="status">Working now · evidence mapper + interview audio review</div></section>
<section class="wrap section" id="prep"><div class="splitTitle"><div><div class="label">LAND</div><h2>Build a role-specific prep map</h2></div><span class="privacyFlag">Local text analysis</span></div><div class="grid"><div class="panel"><div class="field"><label for="resume">Resume / accomplishment source</label><textarea id="resume" spellcheck="false" placeholder="Paste resume text, accomplishment notes, or verified career evidence."></textarea></div><div class="field"><label for="job">Job description / role listing</label><textarea id="job" spellcheck="false" placeholder="Paste the job listing or the important parts of it."></textarea></div><div class="field"><label for="stage">Interview stage</label><select id="stage"><option>Recruiter screen</option><option>Hiring manager</option><option>Panel interview</option><option>Final round</option><option>Executive interview</option></select></div><label class="check"><input id="saveStats" type="checkbox"> Save only run-level statistics in this browser. Raw resume and job text are not saved by this option.</label><div class="actions"><button class="btn primary" id="generate">Generate prep map</button><button class="btn ghost" id="clear">Clear</button></div><div class="note">Keep every claim truthful. Missing evidence stays visible instead of being invented.</div></div><div class="panel"><div class="label">Output</div><h2>Evidence coverage</h2><div id="empty" class="muted">Add resume and role text, then generate a map.</div><div id="output" class="result hidden"><div class="metricrow"><div class="metric"><b id="coverage">0%</b><span>requirements with evidence</span></div><div class="metric"><b id="requirements">0</b><span>requirements extracted</span></div><div class="metric"><b id="evidence">0</b><span>evidence lines used</span></div><div class="metric"><b id="stageMetric">—</b><span>interview stage</span></div></div><div><h3>Requirement → evidence map</h3><div id="map"></div></div><div><h3>STAR retrieval queue</h3><div id="stars"></div></div><div><h3>Evidence gaps</h3><div id="gaps"></div></div></div><div class="history"><h3>Local run history</h3><div id="history">No locally saved run statistics.</div></div></div></div></section>
<section class="wrap section" id="review"><div class="splitTitle"><div><div class="label">INTERVIEW REVIEW</div><h2>Record live or upload audio. Review words + delivery signals.</h2></div><span class="privacyFlag">Upload only on Analyze</span></div><p class="lede">For live capture, LandThePlane can record shared desktop/system audio and your microphone as separate audio streams when the browser exposes them. Your microphone track is used for candidate-specific delivery measurements; system audio supplies interviewer/context. No video is recorded.</p><div class="grid" style="margin-top:14px"><div class="panel"><h3>Live interview capture</h3><label class="check"><input id="recordConsent" type="checkbox"> I have permission to record this conversation and will follow applicable law, employer policy, and meeting rules.</label><label class="check"><input id="recordMic" type="checkbox" checked> Also record my microphone separately for candidate-specific voice analysis.</label><div class="recordbar"><span id="recordDot" class="dot"></span><span id="recordStatus">Not recording</span><span id="recordTimer" class="timer">00:00</span></div><div class="actions"><button class="btn primary" id="startRecord">Start desktop audio capture</button><button class="btn danger" id="stopRecord" disabled>Stop recording</button></div><div id="captureMessage" class="note">When the share dialog opens, choose a source that includes system/tab audio. Browser and operating-system support varies. Video permission may appear because screen-capture APIs require a shared surface, but LandThePlane records only audio tracks.</div><div id="recordingPreview" class="result hidden"></div><hr style="border:0;border-top:1px solid #263442;margin:20px 0"><h3>Or upload an audio recording</h3><div class="field"><label for="uploadType">Recording type</label><select id="uploadType"><option value="candidate">Candidate-only audio</option><option value="mixed">Full / mixed interview recording</option></select></div><div class="field"><label for="audioUpload">Audio file</label><input id="audioUpload" type="file" accept="audio/*,.webm,.wav,.mp3,.m4a,.ogg"><small>Maximum 12 MB per audio track in this alpha.</small></div></div><div class="panel"><h3>Review context</h3><div class="field"><label for="reviewJob">Target job description</label><textarea class="compact" id="reviewJob" placeholder="Paste the role listing or key requirements."></textarea></div><div class="field"><label for="reviewResume">Resume / evidence context</label><textarea class="compact" id="reviewResume" placeholder="Optional. Used to evaluate whether answers were grounded in your actual evidence."></textarea></div><div class="field"><label for="reviewStage">Interview stage</label><select id="reviewStage"><option>Recruiter screen</option><option>Hiring manager</option><option>Panel interview</option><option>Final round</option><option>Executive interview</option></select></div><div class="field"><label for="reviewNotes">Optional notes</label><textarea class="compact" id="reviewNotes" placeholder="Anything the scoring system should know."></textarea></div><div class="actions"><button class="btn ghost" id="copyPrepContext">Use prep context</button><button class="btn primary" id="analyzeInterview">Analyze interview</button></div><div id="analysisState" class="note">Audio remains in your browser until you click Analyze interview. Analysis sends selected audio and review context to this LandThePlane Worker for transcription and AI review. The Worker does not persist the audio or transcript.</div></div></div><div id="audioSignals" class="panel hidden" style="margin-top:12px"><div class="splitTitle"><div><div class="label">Measured voice signals</div><h2>Observable delivery features</h2></div><span id="signalScope" class="privacyFlag">Candidate track</span></div><div id="signalMetrics" class="metricrow"></div><div class="note">These are acoustic coaching signals, not personality or emotion detection. Pitch and volume variation describe the recording signal only.</div></div><div id="aiReview" class="panel hidden" style="margin-top:12px"><div class="splitTitle"><div><div class="label">AI interview review</div><h2>Evidence, fit, and delivery</h2></div><span class="privacyFlag">Cloudflare Workers AI</span></div><div id="reviewScores" class="metricrow"></div><div id="reviewSummary" class="result"></div><div class="grid" style="margin-top:12px"><div><h3>Strengths</h3><div id="reviewStrengths"></div></div><div><h3>Improve next</h3><div id="reviewImprovements"></div></div></div><div class="grid" style="margin-top:12px"><div><h3>Role-fit signals</h3><div id="fitSignals"></div></div><div><h3>Delivery observations</h3><div id="deliveryObservations"></div></div></div><div style="margin-top:12px"><h3>Practice next</h3><div id="practiceNext"></div></div><details style="margin-top:14px"><summary>Candidate transcript</summary><div id="candidateTranscript" class="transcript"></div></details><details style="margin-top:10px"><summary>Interviewer / mixed-context transcript</summary><div id="contextTranscript" class="transcript"></div></details></div></section>
<section class="wrap section" id="product"><div class="label">CAREER OS</div><h2>LAND → RAMP → OPERATE → IMPROVE → PROVE → PIVOT</h2><div class="cards"><article class="card"><h3>Ramp after the offer</h3><p>Turn the job description into version 0 of a living 30/60/90 plan, then replace assumptions with actual work context, stakeholder expectations, and feedback.</p></article><article class="card"><h3>Learn from work</h3><p>Future opt-in email and meeting integrations can turn assignments, commitments, decisions, recurring work, and feedback into a private work graph and personal workflow system.</p></article><article class="card"><h3>Prove + pivot</h3><p>Accumulate verified impact for reviews and promotions, then use capability adjacency to surface credible next roles that title-based career searches might miss.</p></article></div></section></main><footer class="wrap footer"><span>LandThePlane · working product name · A Clintware product.</span><span><a href="/privacy/">Privacy</a> · <a href="/terms/">Terms</a> · <a href="https://www.clintware.com/">Clintware ↗</a></span></footer>`;
  const script = `<script>
(function(){
'use strict';
const STOP=new Set('the a an and or but if then than to of in on for with from by as at is are was were be been being this that these those it its their your you we our they them into about over under through across using use used ability abilities skills skill experience experienced required preferred requirements responsibilities responsibility role work working candidate candidates team teams strong excellent including plus must should will can years year'.split(' '));
const $=id=>document.getElementById(id),resume=$('resume'),job=$('job'),stage=$('stage'),saveStats=$('saveStats');
const clean=s=>(s||'').toLowerCase().replace(/[^a-z0-9+#.%\\- ]+/g,' ').replace(/\\s+/g,' ').trim();
const tokens=s=>clean(s).split(' ').filter(w=>w.length>2&&!STOP.has(w));
const unique=a=>Array.from(new Set(a));
const esc=s=>String(s==null?'':s).replace(/[&<>"']/g,c=>({'&':'&amp;','<':'&lt;','>':'&gt;','"':'&quot;',"'":'&#39;'}[c]));
function lines(text){return String(text||'').split(/\\n|[•·]|(?<=[.!?])\\s+/).map(s=>s.trim()).filter(s=>s.length>18&&s.length<420)}
function metric(line){const m=String(line||'').match(/(?:\\$?[0-9][0-9,.]*\\s?(?:%|k|m|b|million|billion|hours?|days?|weeks?|months?|years?|customers?|accounts?|arr|users?)?)/i);return m?m[0]:''}
function requirementScore(line){const lower=line.toLowerCase();let s=0;if(/must|required|responsib|experience|ability|proficien|manage|lead|build|develop|own|deliver|support|drive|implement/.test(lower))s+=3;s+=Math.min(tokens(line).length,12)/12;return s}
function extractRequirements(text){const ls=lines(text).map(x=>({text:x,score:requirementScore(x)})).sort((a,b)=>b.score-a.score),out=[],seen=new Set();for(const x of ls){const key=unique(tokens(x.text)).slice(0,6).join('|');if(!key||seen.has(key))continue;seen.add(key);out.push(x.text);if(out.length===8)break}return out}
function match(req,evidence){const rt=unique(tokens(req)),et=new Set(tokens(evidence));let score=0;for(const t of rt){if(et.has(t))score+=1;if(t.length>6&&Array.from(et).some(e=>e.startsWith(t.slice(0,5))||t.startsWith(e.slice(0,5))))score+=.35}if(metric(evidence))score+=.6;return score/Math.max(rt.length,4)}
function renderPrep(){const rtext=resume.value.trim(),jtext=job.value.trim();if(rtext.length<40||jtext.length<40){alert('Add enough resume and job text to build a useful map.');return}const reqs=extractRequirements(jtext),ev=lines(rtext).slice(0,80),mapped=reqs.map(req=>{const ranked=ev.map(line=>({line:line,score:match(req,line)})).sort((a,b)=>b.score-a.score);return{req:req,best:ranked[0]||{line:'',score:0}}}),covered=mapped.filter(x=>x.best.score>=.16),used=unique(covered.map(x=>x.best.line)),coverage=reqs.length?Math.round(100*covered.length/reqs.length):0;$('coverage').textContent=coverage+'%';$('requirements').textContent=String(reqs.length);$('evidence').textContent=String(used.length);$('stageMetric').textContent=stage.value.replace(' interview','');$('map').innerHTML=mapped.map(x=>'<div class="item '+(x.best.score>=.16?'ok':'gap')+'"><strong>'+esc(x.req)+'</strong><p>'+(x.best.score>=.16?esc(x.best.line):'No strong evidence match found. Add or verify a supporting accomplishment before the interview.')+'</p></div>').join('');$('stars').innerHTML=used.slice(0,5).map(line=>'<div class="item"><strong>Evidence anchor</strong><p>'+esc(line)+'</p><p><b>STAR shell:</b> Situation/context → task/ownership → action → result'+(metric(line)?' ('+esc(metric(line))+')':' / metric to verify')+' → role link.</p></div>').join('')||'<div class="item gap"><p>Add quantified accomplishments to build the story queue.</p></div>';const gaps=mapped.filter(x=>x.best.score<.16);$('gaps').innerHTML=gaps.length?gaps.map(x=>'<div class="item gap"><strong>'+esc(x.req)+'</strong><p>Prepare a truthful bridge, adjacent example, or explicit learning plan.</p></div>').join(''):'<div class="item ok"><p>No major evidence gaps in this local pass.</p></div>';$('empty').classList.add('hidden');$('output').classList.remove('hidden');if(saveStats.checked){const history=JSON.parse(localStorage.getItem('ltp_runs')||'[]');history.unshift({date:new Date().toISOString(),stage:stage.value,coverage:coverage,requirements:reqs.length,evidence:used.length});localStorage.setItem('ltp_runs',JSON.stringify(history.slice(0,12)));renderHistory()}}
function renderHistory(){const history=JSON.parse(localStorage.getItem('ltp_runs')||'[]');$('history').innerHTML=history.length?history.map(x=>'<div>'+esc(new Date(x.date).toLocaleString())+' · '+esc(x.stage)+' · '+esc(x.coverage)+'% coverage · '+esc(x.evidence)+' evidence lines</div>').join(''):'No locally saved run statistics.'}
$('generate').addEventListener('click',renderPrep);$('clear').addEventListener('click',()=>{resume.value='';job.value='';$('output').classList.add('hidden');$('empty').classList.remove('hidden')});renderHistory();
let displayStream=null,micStream=null,desktopRecorder=null,micRecorder=null,desktopChunks=[],micChunks=[],desktopBlob=null,micBlob=null,recordStarted=0,timerId=null;
function chooseMime(){for(const x of ['audio/webm;codecs=opus','audio/webm','audio/ogg;codecs=opus'])if(window.MediaRecorder&&MediaRecorder.isTypeSupported(x))return x;return ''}
function makeRecorder(stream,bps){const mime=chooseMime(),opts={audioBitsPerSecond:bps};if(mime)opts.mimeType=mime;return new MediaRecorder(stream,opts)}
function fmtTime(sec){sec=Math.max(0,Math.floor(sec));return String(Math.floor(sec/60)).padStart(2,'0')+':'+String(sec%60).padStart(2,'0')}
function setCaptureStatus(text,live){$('recordStatus').textContent=text;$('recordDot').classList.toggle('live',!!live)}
function resetTimer(){if(timerId)clearInterval(timerId);timerId=null;$('recordTimer').textContent='00:00'}
async function startCapture(){if(!$('recordConsent').checked){alert('Confirm that you have permission to record before starting.');return}if(!navigator.mediaDevices||!navigator.mediaDevices.getDisplayMedia||!window.MediaRecorder){alert('This browser does not expose the required screen-audio recording APIs. Use audio upload instead.');return}desktopBlob=null;micBlob=null;desktopChunks=[];micChunks=[];$('recordingPreview').classList.add('hidden');try{displayStream=await navigator.mediaDevices.getDisplayMedia({video:true,audio:true});const audioTracks=displayStream.getAudioTracks();if(!audioTracks.length){displayStream.getTracks().forEach(t=>t.stop());displayStream=null;throw new Error('The selected share source did not provide an audio track. Choose a source with audio sharing enabled.')}desktopRecorder=makeRecorder(new MediaStream(audioTracks),64000);desktopRecorder.ondataavailable=e=>{if(e.data&&e.data.size)desktopChunks.push(e.data)};desktopRecorder.start(1000);if($('recordMic').checked){try{micStream=await navigator.mediaDevices.getUserMedia({audio:{echoCancellation:true,noiseSuppression:true,autoGainControl:true}});micRecorder=makeRecorder(new MediaStream(micStream.getAudioTracks()),48000);micRecorder.ondataavailable=e=>{if(e.data&&e.data.size)micChunks.push(e.data)};micRecorder.start(1000)}catch(err){micStream=null;micRecorder=null;$('captureMessage').textContent='Desktop audio is recording, but microphone access failed. Candidate-specific voice scoring will be limited.'}}recordStarted=Date.now();setCaptureStatus('Recording audio',true);$('startRecord').disabled=true;$('stopRecord').disabled=false;timerId=setInterval(()=>{$('recordTimer').textContent=fmtTime((Date.now()-recordStarted)/1000)},500);displayStream.getVideoTracks().forEach(track=>{track.enabled=false;track.addEventListener('ended',()=>{if(desktopRecorder&&desktopRecorder.state==='recording')stopCapture()},{once:true})})}catch(err){setCaptureStatus('Not recording',false);$('startRecord').disabled=false;$('stopRecord').disabled=true;$('captureMessage').textContent=err&&err.message?err.message:'Unable to start capture.'}}
function stopOne(rec){return new Promise(resolve=>{if(!rec||rec.state==='inactive'){resolve();return}const old=rec.onstop;rec.onstop=e=>{if(typeof old==='function')old(e);resolve()};rec.stop()})}
async function stopCapture(){if(!desktopRecorder&&!micRecorder)return;$('stopRecord').disabled=true;await Promise.all([stopOne(desktopRecorder),stopOne(micRecorder)]);const dType=desktopChunks[0]?desktopChunks[0].type:'audio/webm',mType=micChunks[0]?micChunks[0].type:'audio/webm';desktopBlob=desktopChunks.length?new Blob(desktopChunks,{type:dType}):null;micBlob=micChunks.length?new Blob(micChunks,{type:mType}):null;if(displayStream)displayStream.getTracks().forEach(t=>t.stop());if(micStream)micStream.getTracks().forEach(t=>t.stop());displayStream=null;micStream=null;desktopRecorder=null;micRecorder=null;resetTimer();setCaptureStatus('Recording ready',false);$('startRecord').disabled=false;renderRecordingPreview();if(micBlob)await showLocalSignals(micBlob,'Candidate microphone');else if(desktopBlob)await showLocalSignals(desktopBlob,'Mixed / desktop audio')}
function formatBytes(n){return n<1024*1024?Math.round(n/1024)+' KB':(n/1024/1024).toFixed(1)+' MB'}
function renderRecordingPreview(){const host=$('recordingPreview');host.innerHTML='';if(desktopBlob){const d=document.createElement('div');d.className='item';d.innerHTML='<strong>Desktop / interviewer context · '+formatBytes(desktopBlob.size)+'</strong>';const a=document.createElement('audio');a.className='audioPreview';a.controls=true;a.src=URL.createObjectURL(desktopBlob);d.appendChild(a);host.appendChild(d)}if(micBlob){const d=document.createElement('div');d.className='item ok';d.innerHTML='<strong>Candidate microphone · '+formatBytes(micBlob.size)+'</strong>';const a=document.createElement('audio');a.className='audioPreview';a.controls=true;a.src=URL.createObjectURL(micBlob);d.appendChild(a);host.appendChild(d)}host.classList.toggle('hidden',!desktopBlob&&!micBlob)}
$('startRecord').addEventListener('click',startCapture);$('stopRecord').addEventListener('click',stopCapture);
function median(arr){if(!arr.length)return 0;const a=arr.slice().sort((x,y)=>x-y),m=Math.floor(a.length/2);return a.length%2?a[m]:(a[m-1]+a[m])/2}function avg(arr){return arr.length?arr.reduce((a,b)=>a+b,0)/arr.length:0}function std(arr,mean){return arr.length?Math.sqrt(arr.reduce((a,b)=>a+(b-mean)*(b-mean),0)/arr.length):0}function rangeScore(x,low,idealLow,idealHigh,high){if(x<=low||x>=high)return 20;if(x>=idealLow&&x<=idealHigh)return 100;if(x<idealLow)return 20+80*(x-low)/(idealLow-low);return 100-80*(x-idealHigh)/(high-idealHigh)}
function estimatePitch(data,start,size,sr){let rms=0;for(let i=0;i<size;i++){const v=data[start+i]||0;rms+=v*v}rms=Math.sqrt(rms/size);if(rms<.01)return 0;const minLag=Math.floor(sr/350),maxLag=Math.min(Math.floor(sr/70),size-2);let bestLag=0,best=0;for(let lag=minLag;lag<=maxLag;lag+=2){let num=0,a2=0,b2=0;for(let i=0;i<size-lag;i+=2){const a=data[start+i]||0,b=data[start+i+lag]||0;num+=a*b;a2+=a*a;b2+=b*b}const c=num/Math.sqrt((a2*b2)||1);if(c>best){best=c;bestLag=lag}}return best>.45&&bestLag?sr/bestLag:0}
async function analyzeAudioLocally(blob){const ctx=new (window.AudioContext||window.webkitAudioContext)();try{const buffer=await blob.arrayBuffer(),decoded=await ctx.decodeAudioData(buffer.slice(0)),data=decoded.getChannelData(0),sr=decoded.sampleRate,duration=decoded.duration,frame=Math.max(1024,Math.floor(sr*.05)),step=Math.max(frame,Math.floor(sr*.20)),rms=[];for(let start=0;start+frame<data.length;start+=step){let sum=0;for(let i=0;i<frame;i+=4){const v=data[start+i];sum+=v*v}rms.push(Math.sqrt(sum/Math.ceil(frame/4)))}const med=median(rms),threshold=Math.max(.004,med*.25),silence=rms.length?rms.filter(x=>x<threshold).length/rms.length:0,mean=avg(rms),rmsStd=std(rms,mean),volumeCv=mean?rmsStd/mean:0;let clip=0,sampled=0;const stride=Math.max(1,Math.floor(data.length/500000));for(let i=0;i<data.length;i+=stride){sampled++;if(Math.abs(data[i])>.985)clip++}const clipping=sampled?clip/sampled:0,pitches=[],pSize=Math.min(Math.floor(sr*.05),4096),pCount=36;for(let k=0;k<pCount;k++){const start=Math.floor((data.length-pSize)*(k+.5)/pCount);if(start>=0&&start+pSize<data.length){const p=estimatePitch(data,start,pSize,sr);if(p>=70&&p<=350)pitches.push(p)}}const pMean=avg(pitches),pitchCv=pMean?std(pitches,pMean)/pMean:0,sSilence=rangeScore(silence,.01,.06,.32,.65),sVolume=rangeScore(volumeCv,.03,.12,.55,1.25),sPitch=pitches.length>=5?rangeScore(pitchCv,.01,.06,.30,.65):55,sClip=Math.max(20,100-Math.min(1,clipping/.02)*80),score=Math.round(.25*sSilence+.30*sVolume+.25*sPitch+.20*sClip);return{duration_seconds:Math.round(duration*10)/10,silence_ratio:Math.round(silence*1000)/1000,volume_variation_cv:Math.round(volumeCv*1000)/1000,pitch_variation_cv:Math.round(pitchCv*1000)/1000,pitch_samples:pitches.length,clipping_ratio:Math.round(clipping*10000)/10000,delivery_signal_score:Math.max(0,Math.min(100,score))}}finally{ctx.close()}}
let currentProsody=null;async function showLocalSignals(blob,scope){$('audioSignals').classList.remove('hidden');$('signalScope').textContent=scope;try{currentProsody=await analyzeAudioLocally(blob);const p=currentProsody,metrics=[['Signal score',p.delivery_signal_score+'/100'],['Duration',fmtTime(p.duration_seconds)],['Silence',Math.round(p.silence_ratio*100)+'%'],['Volume variation',p.volume_variation_cv.toFixed(2)],['Pitch variation',p.pitch_samples>=5?p.pitch_variation_cv.toFixed(2):'limited'],['Clipping',(p.clipping_ratio*100).toFixed(2)+'%']];$('signalMetrics').innerHTML=metrics.map(x=>'<div class="metric"><b>'+esc(x[1])+'</b><span>'+esc(x[0])+'</span></div>').join('')}catch(err){currentProsody=null;$('signalMetrics').innerHTML='<div class="item warn"><p>Local acoustic analysis was not available for this audio format in this browser. AI transcription/review can still be attempted.</p></div>'}}
$('audioUpload').addEventListener('change',async()=>{const f=$('audioUpload').files&&$('audioUpload').files[0];if(!f)return;if(f.size>12*1024*1024){alert('Audio file is larger than the 12 MB alpha limit.');$('audioUpload').value='';return}await showLocalSignals(f,$('uploadType').value==='candidate'?'Candidate-only upload':'Mixed recording')});$('uploadType').addEventListener('change',()=>{const f=$('audioUpload').files&&$('audioUpload').files[0];if(f)showLocalSignals(f,$('uploadType').value==='candidate'?'Candidate-only upload':'Mixed recording')});$('copyPrepContext').addEventListener('click',()=>{$('reviewJob').value=job.value;$('reviewResume').value=resume.value;$('reviewStage').value=stage.value});
function listHtml(arr,kind){return (Array.isArray(arr)&&arr.length?arr:['No item returned.']).map(x=>'<div class="'+(kind||'item')+'"><p>'+esc(x)+'</p></div>').join('')}
async function analyzeInterview(){const file=$('audioUpload').files&&$('audioUpload').files[0];let candidateAudio=null,contextAudio=null,scope='';if(file){if($('uploadType').value==='candidate'){candidateAudio=file;scope='candidate'}else{contextAudio=file;scope='mixed'}}else{candidateAudio=micBlob;contextAudio=desktopBlob;scope=micBlob?'separate':'mixed'}if(!candidateAudio&&!contextAudio){alert('Record an interview or choose an audio file first.');return}if(candidateAudio&&candidateAudio.size>12*1024*1024||contextAudio&&contextAudio.size>12*1024*1024){alert('One of the audio tracks exceeds the 12 MB alpha limit.');return}const btn=$('analyzeInterview');btn.disabled=true;$('analysisState').className='note';$('analysisState').textContent='Transcribing audio and building the interview review…';try{if(candidateAudio)await showLocalSignals(candidateAudio,scope==='separate'?'Candidate microphone':'Candidate-only audio');else if(contextAudio&&!currentProsody)await showLocalSignals(contextAudio,'Mixed recording');const fd=new FormData();if(candidateAudio)fd.append('candidate_audio',candidateAudio,candidateAudio.name||'candidate.webm');if(contextAudio)fd.append('context_audio',contextAudio,contextAudio.name||'context.webm');fd.append('job',$('reviewJob').value);fd.append('resume',$('reviewResume').value);fd.append('stage',$('reviewStage').value);fd.append('notes',$('reviewNotes').value);fd.append('audio_scope',scope);if(currentProsody)fd.append('prosody',JSON.stringify(currentProsody));const res=await fetch('/api/interview/analyze',{method:'POST',body:fd,headers:{'X-LandThePlane-Client':'audio-review-v1'}}),data=await res.json();if(!res.ok)throw new Error(data.error||'Interview analysis failed.');renderReview(data);$('analysisState').textContent='Review complete. Audio and transcript were processed for this request and are not saved by the LandThePlane Worker.'}catch(err){$('analysisState').className='note error';$('analysisState').textContent=err&&err.message?err.message:'Interview analysis failed.'}finally{btn.disabled=false}}
$('analyzeInterview').addEventListener('click',analyzeInterview);
function renderReview(data){$('aiReview').classList.remove('hidden');const r=data.review||{},scores=[['Overall',data.scores&&data.scores.overall_score!=null?data.scores.overall_score+'/100':'—'],['Content',r.content_score!=null?r.content_score+'/100':'—'],['Role fit',r.role_fit_score!=null?r.role_fit_score+'/100':'—'],['Delivery',data.scores&&data.scores.delivery_score!=null?data.scores.delivery_score+'/100':'—']];$('reviewScores').innerHTML=scores.map(x=>'<div class="metric"><b>'+esc(x[1])+'</b><span>'+esc(x[0])+'</span></div>').join('');$('reviewSummary').innerHTML='<div class="item ok"><strong>Review summary</strong><p>'+esc(r.summary||'No summary returned.')+'</p></div>'+(data.mixed_audio_warning?'<div class="item warn"><strong>Mixed-speaker limitation</strong><p>'+esc(data.mixed_audio_warning)+'</p></div>':'');$('reviewStrengths').innerHTML=listHtml(r.strengths,'item ok');$('reviewImprovements').innerHTML=listHtml(r.improvements,'item warn');$('fitSignals').innerHTML=listHtml(r.role_fit_signals,'item');$('deliveryObservations').innerHTML=listHtml(r.delivery_observations,'item');$('practiceNext').innerHTML=listHtml(r.practice_next,'item');$('candidateTranscript').textContent=data.transcripts&&data.transcripts.candidate?data.transcripts.candidate:'No separate candidate transcript.';$('contextTranscript').textContent=data.transcripts&&data.transcripts.context?data.transcripts.context:'No separate interviewer/context transcript.';$('aiReview').scrollIntoView({behavior:'smooth',block:'start'})}
})();
</script>`;
  return pageShell(PRODUCT + ' · Public alpha', body, script);
}

function renderPrivacy() {
  const body = `<header class="wrap top"><a class="brand" href="/">LAND<b>THE</b>PLANE</a><nav class="nav"><a href="/">Alpha</a><a href="/terms/">Terms</a></nav></header><main class="wrap hero"><div class="eyebrow">PRIVACY</div><h1>Local by default. Explicit upload for AI review.</h1><p class="lede">The prep mapper runs in your browser. Raw resume/job text is not sent to LandThePlane by that local mapping action. Interview audio is different: when you explicitly click Analyze interview, the selected audio and review context are sent to the LandThePlane Worker and processed with Cloudflare Workers AI for transcription and review.</p></main><section class="wrap section"><div class="cards"><article class="card"><h3>Interview audio</h3><p>The current Worker does not write uploaded audio, transcripts, or AI review output to a database, object store, KV, or other persistent LandThePlane storage. Processing occurs for the request and the result is returned to your browser.</p></article><article class="card"><h3>Local voice signals</h3><p>Duration, silence ratio, signal variation, pitch variation, and clipping are computed in the browser when the audio format can be decoded locally. These are acoustic coaching signals, not emotion or personality detection.</p></article><article class="card"><h3>Recording permission</h3><p>Use recording only when you have permission and when it complies with applicable law, employer policy, confidentiality obligations, and meeting rules.</p></article></div><div class="note">Do not upload confidential employer information unless you are authorized to process it with this service.</div></section><footer class="wrap footer"><span>A Clintware product.</span><span><a href="/">Alpha</a> · <a href="/terms/">Terms</a></span></footer>`;
  return pageShell(PRODUCT + ' · Privacy', body);
}

function renderTerms() {
  const body = `<header class="wrap top"><a class="brand" href="/">LAND<b>THE</b>PLANE</a><nav class="nav"><a href="/">Alpha</a><a href="/privacy/">Privacy</a></nav></header><main class="wrap hero"><div class="eyebrow">ALPHA TERMS</div><h1>Career coaching support, not a hiring guarantee.</h1><p class="lede">LandThePlane helps organize evidence, review interview communication, and identify coaching opportunities. Scores are directional coaching aids, not validated employment assessments, psychological measurements, or predictions of a hiring decision.</p></main><section class="wrap section"><div class="cards"><article class="card"><h3>Your responsibility</h3><p>Keep career claims truthful, verify AI output before relying on it, and do not use the product to fabricate experience or misrepresent qualifications.</p></article><article class="card"><h3>Audio scoring</h3><p>Voice scores are heuristic combinations of observable audio characteristics and transcript context. Recording quality, multiple speakers, background noise, microphone processing, and incomplete context can materially affect results.</p></article><article class="card"><h3>Recording</h3><p>You are responsible for obtaining any required consent and complying with recording, workplace, confidentiality, and data-handling rules.</p></article></div></section><footer class="wrap footer"><span>A Clintware product.</span><span><a href="/">Alpha</a> · <a href="/privacy/">Privacy</a></span></footer>`;
  return pageShell(PRODUCT + ' · Terms', body);
}

function clampText(value, max) { return String(value || '').slice(0, max); }
function isFile(value) { return value && typeof value === 'object' && typeof value.arrayBuffer === 'function' && typeof value.size === 'number'; }
async function transcribe(file, env) {
  if (!isFile(file) || file.size === 0) return '';
  if (file.size > MAX_FILE_BYTES) throw new Error('Audio track exceeds the 12 MB alpha limit.');
  const bytes = await file.arrayBuffer();
  const base64 = Buffer.from(bytes).toString('base64');
  const result = await env.AI.run(TRANSCRIBE_MODEL, { audio: base64, task: 'transcribe', vad_filter: true, condition_on_previous_text: false, no_speech_threshold: 0.65 });
  return clampText(result && (result.text || result.transcription_info && result.transcription_info.text), 36000);
}
function parseProsody(raw) {
  try { const p = JSON.parse(String(raw || '{}')); return { duration_seconds:Number(p.duration_seconds)||0, silence_ratio:Number(p.silence_ratio)||0, volume_variation_cv:Number(p.volume_variation_cv)||0, pitch_variation_cv:Number(p.pitch_variation_cv)||0, pitch_samples:Number(p.pitch_samples)||0, clipping_ratio:Number(p.clipping_ratio)||0, delivery_signal_score:Math.max(0,Math.min(100,Number(p.delivery_signal_score)||0)) }; } catch { return null; }
}
function paceScore(words, seconds) { if(!seconds||!words)return null;const wpm=words/(seconds/60);let score;if(wpm>=115&&wpm<=185)score=100;else if(wpm<115)score=Math.max(35,100-(115-wpm)*1.2);else score=Math.max(35,100-(wpm-185)*.9);return{wpm:Math.round(wpm),score:Math.round(score)}; }
function normalizeReview(review) {
  const obj=review&&typeof review==='object'?review:{},score=key=>Math.max(0,Math.min(100,Math.round(Number(obj[key])||0))),list=key=>Array.isArray(obj[key])?obj[key].map(x=>clampText(x,420)).slice(0,8):[];
  return { content_score:score('content_score'), role_fit_score:score('role_fit_score'), summary:clampText(obj.summary,900), strengths:list('strengths'), improvements:list('improvements'), role_fit_signals:list('role_fit_signals'), evidence_used:list('evidence_used'), missed_opportunities:list('missed_opportunities'), delivery_observations:list('delivery_observations'), practice_next:list('practice_next'), questions_detected:list('questions_detected') };
}
async function aiReview(input, env) {
  const schema={type:'object',properties:{content_score:{type:'integer',minimum:0,maximum:100},role_fit_score:{type:'integer',minimum:0,maximum:100},summary:{type:'string'},strengths:{type:'array',items:{type:'string'}},improvements:{type:'array',items:{type:'string'}},role_fit_signals:{type:'array',items:{type:'string'}},evidence_used:{type:'array',items:{type:'string'}},missed_opportunities:{type:'array',items:{type:'string'}},delivery_observations:{type:'array',items:{type:'string'}},practice_next:{type:'array',items:{type:'string'}},questions_detected:{type:'array',items:{type:'string'}}},required:['content_score','role_fit_score','summary','strengths','improvements','role_fit_signals','evidence_used','missed_opportunities','delivery_observations','practice_next','questions_detected']};
  const system=`You are LandThePlane's interview-review engine. Evaluate a candidate-side interview for coaching only. Score the quality of the candidate's demonstrated interview content and evidence alignment to the supplied role. Do not predict a hiring decision. Do not infer protected traits, mental state, emotion, personality, health, or honesty from voice. Delivery observations may use only measured acoustic metrics supplied by the application, such as pace, silence ratio, volume variation, pitch variation, clipping, and recording scope. If the recording is mixed-speaker, explicitly reduce certainty about delivery. Treat the job description as requirements, not proof of candidate experience. Treat resume/evidence context as candidate-supplied evidence, not independently verified fact. Prefer specific, actionable observations.`;
  const user=`INTERVIEW STAGE\n${input.stage}\n\nTARGET ROLE / JOB DESCRIPTION\n${input.job||'[not supplied]'}\n\nRESUME / EVIDENCE CONTEXT\n${input.resume||'[not supplied]'}\n\nCANDIDATE TRANSCRIPT\n${input.candidateTranscript||'[no separate candidate transcript]'}\n\nINTERVIEWER OR MIXED CONTEXT TRANSCRIPT\n${input.contextTranscript||'[not supplied]'}\n\nAUDIO SCOPE\n${input.audioScope}\n\nMEASURED ACOUSTIC SIGNALS\n${JSON.stringify(input.prosody||{})}\n\nUSER NOTES\n${input.notes||'[none]'}\n\nReturn content_score and role_fit_score from 0-100. Base role fit on evidence actually demonstrated in the interview relative to the supplied role. Strengths and improvements must be specific. Delivery observations must describe only measurable communication features, never personality or emotion.`;
  const result=await env.AI.run(REVIEW_MODEL,{messages:[{role:'system',content:system},{role:'user',content:user}],max_tokens:1800,temperature:.2,response_format:{type:'json_schema',json_schema:schema}});
  return normalizeReview(result&&result.response);
}
async function handleInterviewAnalyze(request, env) {
  if(!env.AI)return jsonResponse({error:'Workers AI is not configured for this deployment.'},503);
  const origin=request.headers.get('Origin'),url=new URL(request.url);if(origin&&origin!==url.origin)return jsonResponse({error:'Cross-origin interview analysis is not allowed.'},403);
  const contentType=request.headers.get('Content-Type')||'';if(!contentType.startsWith('multipart/form-data'))return jsonResponse({error:'Expected multipart audio upload.'},415);
  const declared=Number(request.headers.get('Content-Length')||0);if(declared&&declared>MAX_TOTAL_BYTES)return jsonResponse({error:'Interview upload exceeds the current alpha request limit.'},413);
  try{
    const form=await request.formData(),candidateFile=form.get('candidate_audio'),contextFile=form.get('context_audio'),candidateSize=isFile(candidateFile)?candidateFile.size:0,contextSize=isFile(contextFile)?contextFile.size:0;
    if(!candidateSize&&!contextSize)return jsonResponse({error:'No audio track was supplied.'},400);
    if(candidateSize>MAX_FILE_BYTES||contextSize>MAX_FILE_BYTES||candidateSize+contextSize>MAX_TOTAL_BYTES)return jsonResponse({error:'Audio exceeds the current alpha size limits.'},413);
    const stage=clampText(form.get('stage'),120),job=clampText(form.get('job'),12000),resume=clampText(form.get('resume'),12000),notes=clampText(form.get('notes'),2000),audioScope=clampText(form.get('audio_scope'),40)||'mixed',prosody=parseProsody(form.get('prosody'));
    let candidateTranscript='',contextTranscript='';if(candidateSize)candidateTranscript=await transcribe(candidateFile,env);if(contextSize)contextTranscript=await transcribe(contextFile,env);
    const reviewCandidateTranscript=candidateTranscript||(audioScope==='mixed'?contextTranscript:'');if(!reviewCandidateTranscript&&!contextTranscript)return jsonResponse({error:'No speech could be transcribed from the supplied audio.'},422);
    const review=await aiReview({stage,job,resume,notes,audioScope,prosody,candidateTranscript:reviewCandidateTranscript,contextTranscript},env),words=reviewCandidateTranscript.trim()?reviewCandidateTranscript.trim().split(/\s+/).length:0,pace=prosody?paceScore(words,prosody.duration_seconds):null;let deliveryScore=null;if(prosody){const base=prosody.delivery_signal_score;deliveryScore=pace?Math.round(base*.72+pace.score*.28):Math.round(base)}const weights=deliveryScore==null?{content:.62,fit:.38,delivery:0}:{content:.48,fit:.32,delivery:.20},overall=Math.round(review.content_score*weights.content+review.role_fit_score*weights.fit+(deliveryScore||0)*weights.delivery),mixedWarning=audioScope==='mixed'?'This recording appears to contain multiple speakers. Content review uses the transcript, but candidate-specific delivery measurements may reflect interviewer audio, room noise, or both. Use a separate microphone track for stronger voice coaching.':'';
    console.log(JSON.stringify({event:'interview_review',candidate_bytes:candidateSize,context_bytes:contextSize,candidate_words:words,scope:audioScope,has_job:Boolean(job),has_resume:Boolean(resume)}));
    return jsonResponse({model:{transcription:TRANSCRIBE_MODEL,review:REVIEW_MODEL},transcripts:{candidate:candidateTranscript||(audioScope==='mixed'?contextTranscript:''),context:contextTranscript},prosody,pace,review,scores:{overall_score:overall,delivery_score:deliveryScore,content_score:review.content_score,role_fit_score:review.role_fit_score},mixed_audio_warning:mixedWarning,scoring_method:'Directional coaching score. Content and role-fit are AI-reviewed from transcripts and supplied evidence. Delivery is a heuristic from observable acoustic metrics and speaking pace when available. It is not a psychological or hiring assessment.'});
  }catch(error){console.error(JSON.stringify({event:'interview_review_error',message:error instanceof Error?error.message:String(error)}));return jsonResponse({error:error instanceof Error?error.message:'Interview analysis failed.'},500)}
}
export default { async fetch(request,env){ const url=new URL(request.url);if(request.method==='POST'&&url.pathname==='/api/interview/analyze')return handleInterviewAnalyze(request,env);if(request.method!=='GET'&&request.method!=='HEAD')return new Response('Method Not Allowed',{status:405,headers:securityHeaders});if(url.pathname==='/healthz')return jsonResponse({ok:true,product:PRODUCT,mode:'local-prep-plus-ai-audio-review',ai:Boolean(env.AI)});if(url.pathname==='/privacy'||url.pathname==='/privacy/')return htmlResponse(renderPrivacy());if(url.pathname==='/terms'||url.pathname==='/terms/')return htmlResponse(renderTerms());if(url.pathname==='/'||url.pathname==='/index.html')return htmlResponse(renderHome());return new Response('Not Found',{status:404,headers:securityHeaders}); } };
