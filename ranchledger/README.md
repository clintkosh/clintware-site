# Clintware Ranch Ledger – Cloudflare Pages build

Cloudflare Pages settings:

- Production branch: `main`
- Root directory: `ranchledger`
- Build command: leave blank
- Build output directory: `.`
- Custom domain: `ranchledger.clintware.com`

The app stores ranch records in the browser and supports JSON backup/restore.
